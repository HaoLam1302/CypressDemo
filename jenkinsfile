pineline{
    agent any
    tools {nodejs "node"}

   
    stages {
        stage('cypress paralell tests'){
            parallel {
                stage('tester A'){
                    steps{
                        sh "npm run cy:parallel"
                    }
                }

              stage('tester B'){
                    steps{
                        sh "npm run cy:parallel"
                    }
                }

                stage('tester C'){
                    steps{
                        sh "npm run cy:parallel"
                    }
                }
            }
        }
    }
}